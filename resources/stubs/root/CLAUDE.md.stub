# AI Harness

This project uses {{ package_name }} to keep Claude Code guidance, hooks, and skills aligned with the local Laravel development harness.

## Runtime

- Use Herd on macOS when available.
- Use Sail/Docker when a Sail stack is active.
- Fall back to bare PHP only when the environment already provides the required PHP runtime.

## Worktrees

Fetch {{ worktree_base_ref }} before creating worktrees, then start new worktrees from {{ worktree_base_ref }} unless the project explicitly overrides that policy.

Run tests through `.dev/bin/ai-harness test` in managed worktrees so the generated local PHPUnit config applies.

Keep custom Claude-specific notes outside this managed block; this block is refreshed by `php artisan ai-harness:update`.
